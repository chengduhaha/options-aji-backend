services:
  options-aji-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./data:/workspace/data
    restart: unless-stopped
