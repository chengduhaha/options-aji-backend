[project]
name = "optionsaji-ontology"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.5.0",
  "pyyaml>=6.0",
  "jsonschema>=4.20"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ontology"]
